"use client"

const Header = () => {
  return (
    <header className="bg-gray-800 text-white pl-5 z-997 fixed top-0 left-0 right-0 flex items-center w-full transition-all duration-500 h-16 shadow-box-background">
      Header
    </header>
  )
}

export default Header
